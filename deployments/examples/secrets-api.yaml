name: secrets-api
image: ghcr.io/example/api:1.0.0
replicas: 2
env:
  NODE_ENV: production
  DATABASE_URL:
    secretRef: prod/database-url
ports:
  - container: 8080
resources:
  cpu: 250m
  memory: 256Mi
restart:
  policy: always
